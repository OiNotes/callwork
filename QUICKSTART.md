# 🚀 Быстрый старт Callwork

## Шаг 1: Создайте .env файл

```bash
cd "/Users/sile/Documents/Status Stock 4.0/Call stat/callwork"
cp .env.example .env
```

Откройте `.env` и заполните:

```env
# Используйте локальный PostgreSQL или создайте бесплатную БД на neon.tech
DATABASE_URL="postgresql://user:password@localhost:5432/callwork"

# Сгенерируйте секрет
NEXTAUTH_SECRET="$(openssl rand -base64 32)"
NEXTAUTH_URL="http://localhost:3000"

# Получите токен у @BotFather в Telegram (или оставьте пустым на первый запуск)
TELEGRAM_BOT_TOKEN="your-bot-token"
```

## Шаг 2: Настройте базу данных

**Вариант A: Локальный PostgreSQL**

```bash
# Создайте БД
psql -U postgres -c "CREATE DATABASE callwork;"

# Примените схему
npx prisma generate
npx prisma db push
```

**Вариант B: Neon.tech (бесплатно, рекомендуется)**

1. Зарегистрируйтесь: https://neon.tech
2. Создайте проект
3. Скопируйте Connection String в DATABASE_URL
4. Выполните:

```bash
npx prisma generate
npx prisma db push
```

## Шаг 3: Запустите приложение

```bash
./start-local.sh
```

Или вручную в двух терминалах:

```bash
# Терминал 1
npm run dev

# Терминал 2
npm run bot:dev
```

## Шаг 4: Откройте браузер

Перейдите на: **http://localhost:3000**

## Шаг 5: Зарегистрируйтесь

1. Нажмите "Register"
2. Заполните форму (имя, email, пароль)
3. Войдите в систему

## Шаг 6: Привяжите Telegram бота (опционально)

1. Создайте бота у @BotFather в Telegram
2. Скопируйте токен в .env
3. Перезапустите бота: `npm run bot:dev`
4. В веб-приложении перейдите в Profile → Generate Code
5. В Telegram отправьте боту `/register` и введите код
6. Готово! Теперь отправляйте отчёты через `/report`

---

## ❓ Проблемы?

### "Cannot connect to database"

- Проверьте DATABASE_URL в .env
- Убедитесь что PostgreSQL запущен (локально)
- Или используйте Neon.tech (облачная БД)

### "NEXTAUTH_SECRET is not set"

```bash
echo "NEXTAUTH_SECRET=\"$(openssl rand -base64 32)\"" >> .env
```

### Telegram бот не отвечает

- Проверьте TELEGRAM_BOT_TOKEN в .env
- Запустите бота: `npm run bot:dev`

### Next.js не запускается

```bash
rm -rf .next node_modules
npm install
npm run dev
```

---

## 📚 Полная документация

См. **README.md** для детальных инструкций.
