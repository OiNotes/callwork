import TelegramBot from 'node-telegram-bot-api'

export async function startHandler(bot: TelegramBot, msg: TelegramBot.Message) {
  const chatId = msg.chat.id

  const welcomeMessage = `
👋 *Добро пожаловать в Callwork Premium!*

Я ваш персональный ассистент для трекинга продаж.
Помогаю держать руку на пульсе и достигать новых высот! 🚀

🔑 *С чего начать:*
1. Зайдите в свой профиль на сайте
2. Скопируйте код привязки
3. Отправьте команду /register

📊 *Мои возможности:*
/register — Подключить аккаунт
/report — Сдать отчёт за день

_Готовы к новым рекордам?_ 💪
  `.trim()

  await bot.sendMessage(chatId, welcomeMessage, { parse_mode: 'Markdown' })
}
