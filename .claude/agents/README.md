# Финансовые Coding Agents для Callwork

Специализированные AI агенты-разработчики для написания финансового кода с нулевой толерантностью к ошибкам в расчётах.

## 🎯 Доступные агенты

### 1. Financial Code Developer 💻
**Файл:** `financial-developer.md`
**Модель:** Claude Sonnet 4.5
**Режим:** Read-Write (полный доступ)

**Когда использовать:**
- Создание новых финансовых компонентов (React, Server Components)
- Разработка API endpoints для расчётов
- Написание финансовых сервисов и утилит
- Реализация фичей с TDD подходом

**Примеры запросов:**
```
Создай API endpoint для расчёта прогноза выручки
Добавь компонент отображения прогресса плана с Decimal.js
Реализуй сервис для расчёта комиссий с тестами
Напиши утилиту для конвертации валют
```

**Экспертиза:**
- ✅ Decimal.js для точных расчётов
- ✅ TDD (тесты перед кодом)
- ✅ Zod валидация
- ✅ Next.js 15, React, Prisma
- ✅ TypeScript strict mode

---

### 2. Financial Bug Fixer 🔧
**Файл:** `financial-bug-fixer.md`
**Модель:** Claude Sonnet 4.5
**Режим:** Read-Edit (только редактирование существующих файлов)

**Когда использовать:**
- Тесты падают в финансовой логике
- Ошибки в расчётах (precision, rounding)
- Несоответствия валют
- Баги в воронке продаж или метриках

**Примеры запросов:**
```
Тест calculateInterest падает - исправь
Баг: 0.1 + 0.2 не равно 0.3 в расчётах
Ошибка: пытаемся сложить USD и EUR
Deals > Contracts в отчётах - нелогично
```

**Экспертиза:**
- ✅ Диагностика floating-point ошибок
- ✅ Root cause analysis (7-phase процесс)
- ✅ Минимальные хирургические исправления
- ✅ Regression тесты обязательны
- ✅ Без scope creep - только fix бага

---

### 3. Financial Architect 🏗️
**Файл:** `financial-architect.md`
**Модель:** Claude Sonnet 4.5
**Режим:** Read-Only (не пишет код)

**Когда использовать:**
- Дизайн новых крупных финансовых фичей
- Рефакторинг финансовой архитектуры
- Создание API контрактов
- Принятие архитектурных решений

**Примеры запросов:**
```
Спроектируй систему multi-currency аккаунтов
Создай ADR для внедрения Event Sourcing
Предложи архитектуру для audit trail
Оптимизируй структуру финансовых сервисов
```

**Экспертиза:**
- ✅ Architecture Decision Records (ADR)
- ✅ API дизайн (REST, idempotency, validation)
- ✅ Data modeling (Prisma схемы)
- ✅ Trade-off анализ
- ✅ Паттерны: Money Object, Repository, CQRS

---

## 🔄 Workflow: как агенты работают вместе

```
User Request: "Добавь функцию расчёта процентов с multi-currency"
    ↓
1. Financial Architect (если новая фича)
   - Анализирует требования
   - Создаёт ADR с дизайном
   - Определяет API контракт
   - Статус: READY_FOR_BUILD
    ↓
2. Financial Developer
   - Пишет failing тесты
   - Реализует код с Decimal.js
   - Все тесты проходят
   - Коммитит
    ↓
3. Financial Bug Fixer (если тесты падают)
   - Диагностирует root cause
   - Минимальный fix
   - Regression test
    ↓
4. Done ✅
```

---

## 💡 Примеры использования

### Пример 1: Новая фича

**Запрос:** "Добавь систему расчёта скидок для клиентов"

```bash
# Шаг 1: Architect проектирует
Architect → ADR создан:
- API: POST /api/discounts/calculate
- Data model: Discount table в Prisma
- Бизнес-правила: validation, currency handling
- Статус: READY_FOR_BUILD

# Шаг 2: Developer реализует
Developer → Код написан:
- ✅ Zod схемы для validation
- ✅ Decimal.js для расчётов
- ✅ 12 тестов (все проходят)
- ✅ API route создан

# Шаг 3: Готово!
npm test → All tests passing ✅
```

---

### Пример 2: Исправление бага

**Запрос:** "Тест падает: expected 0.3, got 0.30000000000000004"

```bash
# Bug Fixer работает:
Bug Fixer → Диагностика:
- Root cause: используется Number вместо Decimal
- Найдена строка: const total = price + tax;
- Fix: const total = new Decimal(price).plus(tax);
- Regression test добавлен

# Проверка:
npm test → All tests passing ✅
git diff → 3 lines changed (minimal)
```

---

### Пример 3: Рефакторинг

**Запрос:** "Оптимизируй структуру финансовых сервисов"

```bash
# Architect анализирует:
Architect → ADR создан:
- Проблема: дублирование logic в 5 файлах
- Решение: Extract MoneyService
- Trade-offs: простота vs гибкость
- План: 3-step migration

# Developer реализует:
Developer → Рефакторинг выполнен:
- MoneyService создан
- 5 файлов обновлено
- Все тесты проходят
- Код чище на 200 строк
```

---

## 🛠️ Helper скрипты

Агенты могут использовать эти скрипты для валидации:

### validate-financial-data.ts
Валидация финансовых данных в системе:
```bash
cd "/Users/sile/Documents/Status Stock 4.0/callstat/callwork"
npx tsx scripts/validate-financial-data.ts
npx tsx scripts/validate-financial-data.ts --detailed
```

**Проверяет:**
- ✅ Точность decimal конверсий
- ✅ Согласованность целей команды
- ✅ Аномалии в отчётах
- ✅ Корректность расчётов метрик

---

### analyze-funnel-health.ts
Анализ здоровья воронки продаж:
```bash
npx tsx scripts/analyze-funnel-health.ts
npx tsx scripts/analyze-funnel-health.ts --detailed
npx tsx scripts/analyze-funnel-health.ts --critical-only
```

**Проверяет:**
- ✅ Конверсии на каждом этапе
- ✅ Красные зоны (< benchmark)
- ✅ Health score команды
- ✅ Критичные проблемы

---

### sync-team-goals.ts
Синхронизация целей команды:
```bash
npx tsx scripts/sync-team-goals.ts --validate
npx tsx scripts/sync-team-goals.ts --manager="id" --distribute
npx tsx scripts/sync-team-goals.ts --user="id" --goal=3000000
```

**Проверяет:**
- ✅ Согласованность целей (team = manager + employees)
- ✅ Все сотрудники имеют цели
- ✅ Реалистичность целей

---

## 📐 Архитектура

```
Callwork Financial System
│
├─ Агенты (.claude/agents/)
│  ├─ financial-developer.md       # Пишет код (TDD)
│  ├─ financial-bug-fixer.md       # Исправляет баги
│  └─ financial-architect.md       # Дизайн и ADR
│
├─ Helper скрипты (scripts/)
│  ├─ validate-financial-data.ts   # Валидация
│  ├─ analyze-funnel-health.ts     # Health check
│  └─ sync-team-goals.ts           # Синхронизация целей
│
├─ Сервисы (lib/services/)
│  ├─ GoalService.ts               # Single Source of Truth для целей
│  └─ SalesMetricsService.ts       # Унифицированные метрики
│
├─ Аналитика (lib/analytics/)
│  └─ funnel.ts                    # Аналитика воронки
│
├─ API Routes (app/api/)
│  ├─ goals/                       # Цели
│  ├─ reports/                     # Отчёты
│  └─ metrics/                     # Метрики
│
└─ Библиотеки
   ├─ decimal.js                   # Точные финансовые расчёты
   └─ zod                          # Валидация схем
```

---

## 🔑 Ключевые принципы

### 1. Decimal Precision (ОБЯЗАТЕЛЬНО)
```typescript
// ✅ ПРАВИЛЬНО
import Decimal from 'decimal.js';
const total = new Decimal(price).plus(tax);

// ❌ НЕПРАВИЛЬНО
const total = price + tax; // Floating-point errors!
```

### 2. Money Class Pattern
```typescript
interface Money {
  amount: Decimal;
  currency: string; // ISO 4217
}

// Всегда проверяй currency match
if (a.currency !== b.currency) {
  throw new CurrencyMismatchError();
}
```

### 3. Test-Driven Development (TDD)
```typescript
// 1. Напиши failing тест
test('calculates interest', () => {
  expect(calculateInterest(100, 0.05)).toBe('5.00');
});

// 2. Реализуй минимальный код
function calculateInterest(principal: number, rate: number): string {
  return new Decimal(principal).times(rate).toFixed(2);
}

// 3. Рефактори
```

### 4. Validation (Multi-Layer)
```typescript
// Layer 1: Zod schema
const schema = AmountSchema.parse(input);

// Layer 2: Business rules
if (schema.amount <= 0) throw new InvalidAmountError();

// Layer 3: Database constraints
// CHECK (amount > 0)
```

### 5. Единый источник данных
```typescript
// ✅ ВСЕГДА используй GoalService
import { GoalService } from '@/lib/services/GoalService';
const goal = await GoalService.getUserGoal(userId);

// ❌ НЕ делай прямых запросов
const goal = await prisma.user.findUnique({ where: { id } });
```

---

## 🚀 Как использовать агентов

### В чате Claude Code:

```bash
# Автоматический вызов (агенты PROACTIVE)
Создай API для расчёта прогноза
→ Financial Developer автоматически активируется

Баг: тест падает в calculateInterest
→ Financial Bug Fixer автоматически активируется

Спроектируй систему скидок
→ Financial Architect автоматически активируется
```

### Явный вызов (если нужно):

```typescript
// Через Task tool (для других агентов)
Task({
  subagent_type: 'financial-developer',
  prompt: 'Реализуй API endpoint для расчёта комиссий'
})
```

---

## ✅ Best Practices

### 1. Используй правильного агента
| Задача | Агент |
|--------|-------|
| Новый код, фичи, компоненты | **Developer** |
| Баги, ошибки, падающие тесты | **Bug Fixer** |
| Дизайн, ADR, рефакторинг | **Architect** |

### 2. Проверяй данные регулярно
```bash
# Еженедельно
npx tsx scripts/validate-financial-data.ts
npx tsx scripts/analyze-funnel-health.ts

# Перед началом месяца
npx tsx scripts/sync-team-goals.ts --validate
```

### 3. Запускай тесты всегда
```bash
# Перед каждым коммитом
npm test                # Все тесты
npm run lint            # Линтер
git diff                # Проверь diff
```

### 4. Доверяй агентам
- ✅ Используют проверенные паттерны
- ✅ Decimal.js обеспечивает точность
- ✅ TDD гарантирует качество
- ✅ Minimal diffs - безопасные изменения

---

## 🎓 Обучение агентов

Агенты обучены на:
- ✅ Claude Code best practices (AgentCoder, FixAgent)
- ✅ Financial domain patterns (Money, Audit Trail, Idempotency)
- ✅ Callwork архитектуре (GoalService, SalesMetricsService)
- ✅ TypeScript/Next.js 15/Prisma best practices

---

## 🔄 Обновления

Агенты хранятся в git:
- ✅ Версионируются
- ✅ Доступны всей команде
- ✅ Можно откатить изменения

Для обновления агента:
```bash
cd "/Users/sile/Documents/Status Stock 4.0/callstat/callwork"
# Отредактируй .claude/agents/{agent}.md
git add .claude/agents/
git commit -m "Update financial agents"
```

---

## 🆘 Troubleshooting

### Агент не активируется автоматически
- Проверь: description содержит "ПРОАКТИВНО" или "use PROACTIVELY"
- Попробуй явный вызов через имя агента в запросе

### Тесты падают после изменений
- Запусти: `npm test -- --verbose`
- Вызови Bug Fixer: "Исправь падающие тесты"

### Сомнения в архитектурном решении
- Вызови Architect: "Создай ADR для этого решения"
- Получишь trade-off анализ и рекомендации

---

## 📚 Документация

- [GoalService](../lib/services/GoalService.ts) - API для работы с целями
- [SalesMetricsService](../lib/services/SalesMetricsService.ts) - API для метрик
- [Prisma Schema](../prisma/schema.prisma) - Database модели
- [Decimal.js Docs](https://mikemcl.github.io/decimal.js/) - Документация библиотеки

---

**Создано:** 2025-11-21
**Версия:** 2.0 (Coding Agents)
**Модель:** Claude Sonnet 4.5
**Библиотеки:** Decimal.js, Zod, Prisma, Next.js 15

---

**Главное отличие от v1.0:** Агенты теперь **пишут код**, а не просто анализируют данные! 💻
