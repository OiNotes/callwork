# Callwork Skills - Краткая справка

Автоматизированные сценарии для типовых задач в Callwork проекте.

## Доступные Skills

### 🚀 Development

#### `quick-start`
Быстрый запуск проекта (установка зависимостей + dev сервер).
```
Используй: при первом запуске или после git pull
```

#### `health-check`
Проверка состояния системы (сервер, БД, API, env vars).
```
Используй: для диагностики проблем или после изменений
```

#### `fix-errors`
Автоматический анализ и исправление типовых ошибок.
```
Используй: когда билд падает или есть TypeScript errors
```

---

### 🧪 Testing & Validation

#### `run-tests`
Запуск всех тестов (аналитика, TypeScript, ESLint).
```
Используй: перед коммитом или после изменений в логике
```

#### `validate-finances`
Валидация финансовых данных и консистентности целей.
```
Используй: после изменений в данных или бизнес-логике
```

---

### 🗄️ Database

#### `db-query`
Безопасное выполнение SQL запросов через Prisma.
```
Используй: для исследования данных или отладки
```

---

## Как использовать Skills

### В Claude Code

Skills активируются автоматически на основе контекста запроса:

```
Пользователь: "Запусти проект"
→ Claude автоматически использует quick-start skill

Пользователь: "Проверь финансовые данные"
→ Claude автоматически использует validate-finances skill
```

### Явный вызов

Можно явно попросить Claude использовать конкретный skill:

```
"Используй skill health-check чтобы проверить систему"
"Запусти validate-finances для проверки целей команды"
```

---

## Примеры использования

### Типовой workflow разработки

1. **Начало работы**
   ```
   "Запусти проект" → quick-start
   ```

2. **Проверка здоровья**
   ```
   "Проверь что всё работает" → health-check
   ```

3. **Разработка фичи**
   ```
   // Используй субагентов:
   // - financial-developer для новых фич
   // - financial-bug-fixer для исправлений
   ```

4. **Перед коммитом**
   ```
   "Запусти тесты" → run-tests
   "Проверь финансы" → validate-finances
   ```

### Debugging workflow

1. **Проблема с билдом**
   ```
   "Исправь ошибки билда" → fix-errors
   ```

2. **Проблема с данными**
   ```
   "Проверь данные в БД" → db-query
   "Валидируй финансовые данные" → validate-finances
   ```

3. **Проблема с окружением**
   ```
   "Проверь health системы" → health-check
   ```

---

## Связь с субагентами

Skills работают совместно с субагентами из `.claude/agents/`:

| Субагент | Используемые Skills |
|----------|-------------------|
| **financial-developer** | quick-start, run-tests, validate-finances |
| **financial-bug-fixer** | fix-errors, run-tests, db-query |
| **financial-architect** | health-check, validate-finances |

---

## Часто задаваемые вопросы

### Когда использовать Skills vs субагентов?

- **Skills** - для автоматизации типовых команд и проверок
- **Субагенты** - для реализации фич, фиксов багов, архитектурных решений

### Можно ли создавать свои Skills?

Да! Создай новый `.md` файл в `.claude/skills/` с YAML frontmatter:

```yaml
---
name: my-skill
description: Что делает этот skill и когда его использовать
allowed-tools: Bash, Read, Edit
---

# My Skill

Инструкции и команды...
```

### Как отладить Skills?

Skills просто выполняют Bash команды, которые описаны в их `.md` файлах. Можешь:
1. Открыть `.claude/skills/<skill>.md`
2. Скопировать команды из секции "Шаги"
3. Запустить вручную в терминале

---

## Быстрая справка команд

```bash
# Development
npm run dev              # Запуск dev сервера
npm run build            # Production build
npx tsc --noEmit        # TypeScript check

# Testing
npm run test:analytics   # Analytics tests
npm run lint            # ESLint

# Database
npx prisma studio       # GUI для БД
npx prisma generate     # Генерация Prisma Client
npx prisma db push      # Применить schema изменения

# Validation Scripts
npx tsx scripts/validate-financial-data.ts --detailed
npx tsx scripts/analyze-funnel-health.ts --detailed
npx tsx scripts/sync-team-goals.ts --validate
```

---

## Дополнительные ресурсы

- **Документация проекта:** `/CLAUDE.md`
- **Субагенты:** `.claude/agents/README.md`
- **Быстрый старт:** `/QUICKSTART.md`
- **Детальное README:** `/README.md`
