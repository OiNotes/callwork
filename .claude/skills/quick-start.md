---
name: quick-start
description: Быстрый запуск Callwork проекта (установка зависимостей, генерация Prisma, запуск dev сервера). Используй при первом запуске или после git pull.
allowed-tools: Bash, Read
---

# Quick Start Skill

Автоматический запуск Callwork проекта для разработки.

## Что делает

1. Проверяет наличие зависимостей
2. Устанавливает npm packages если нужно
3. Генерирует Prisma Client
4. Запускает dev сервер на порту 3000

## Шаги

### 1. Проверка node_modules
```bash
if [ ! -d "node_modules" ]; then
  echo "📦 Устанавливаю зависимости..."
  npm install
else
  echo "✅ node_modules уже установлены"
fi
```

### 2. Генерация Prisma Client
```bash
echo "🔧 Генерирую Prisma Client..."
npx prisma generate
```

### 3. Запуск dev сервера
```bash
echo "🚀 Запускаю dev сервер..."
npm run dev
```

## Результат

- Dev сервер доступен на http://localhost:3000
- Hot reload включён
- Prisma Client сгенерирован

## Когда использовать

- Первый запуск проекта
- После `git pull` с изменениями в package.json
- После изменений в prisma/schema.prisma
- После переустановки зависимостей
