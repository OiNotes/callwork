---
name: run-tests
description: Запуск всех тестов Callwork (аналитика, финансовые расчёты). Используй перед коммитом или после изменений в бизнес-логике.
allowed-tools: Bash, Read
---

# Run Tests Skill

Запуск тестового окружения для Callwork.

## Что делает

1. Запускает аналитические тесты
2. Проверяет TypeScript компиляцию
3. Запускает ESLint
4. Выводит summary результатов

## Шаги

### 1. TypeScript type checking
```bash
echo "🔍 Проверяю типы TypeScript..."
npx tsc --noEmit
```

### 2. Запуск analytics tests
```bash
echo "📊 Запускаю тесты аналитики..."
npm run test:analytics
```

### 3. Lint code
```bash
echo "🧹 Проверяю code style..."
npm run lint
```

## Результат

- ✅ Все тесты прошли
- ✅ TypeScript без ошибок
- ✅ ESLint без warning

Или:

- ❌ Список failed тестов
- ❌ TypeScript errors
- ❌ Lint violations

## Когда использовать

- Перед git commit
- После изменений в lib/analytics/
- После изменений в lib/services/
- После изменений в финансовых расчётах
- В CI/CD pipeline
