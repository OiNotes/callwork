---
name: validate-finances
description: Валидация финансовых данных и консистентности целей (проверка Decimal точности, team goals sync, funnel health). Используй после изменений в данных или бизнес-логике.
allowed-tools: Bash, Read
---

# Validate Finances Skill

Комплексная проверка финансовых данных и целостности системы.

## Что делает

1. Проверяет точность Decimal расчётов
2. Валидирует синхронизацию team goals
3. Анализирует воронку продаж (funnel health)
4. Проверяет консистентность БД

## Шаги

### 1. Financial Data Validation
```bash
echo "💰 Проверяю финансовые данные..."
npx tsx scripts/validate-financial-data.ts --detailed
```

Проверяет:
- Decimal precision (нет floating point errors)
- Currency consistency
- Отсутствие отрицательных значений
- Математическая корректность сумм

### 2. Team Goals Sync
```bash
echo "🎯 Валидирую синхронизацию целей..."
npx tsx scripts/sync-team-goals.ts --validate
```

Проверяет invariant:
```
team_goal = manager_goal + sum(employee_goals)
```

### 3. Funnel Health Analysis
```bash
echo "📊 Анализирую воронку продаж..."
npx tsx scripts/analyze-funnel-health.ts --detailed
```

Проверяет:
- Конверсии по этапам vs бенчмарки
- Red zones (критичные провалы)
- North Star KPI (Zoom1 → Deal = 5%)

## Результат

### ✅ Успешная валидация:
```
✓ Все Decimal расчёты корректны
✓ Team goals синхронизированы
✓ Funnel health: HEALTHY (score > 80)
✓ Нет критичных аномалий
```

### ❌ Найдены проблемы:
```
✗ Precision errors в Report ID 123
✗ Team goal mismatch: manager_123
✗ RED ZONE: zoom1ToZoom2 = 35% (норма 50%)
✗ North Star KPI = 3.2% (норма 5%)
```

## Когда использовать

- После bulk импорта данных
- После изменений в GoalService
- Перед production deployment
- При подозрении на data inconsistency
- Еженедельно для мониторинга здоровья системы

## Дополнительные флаги

```bash
# Только критичные ошибки
npx tsx scripts/analyze-funnel-health.ts --critical-only

# Установить новую цель пользователю
npx tsx scripts/sync-team-goals.ts --user="user_id" --goal=3000000

# Распределить цель команды между сотрудниками
npx tsx scripts/sync-team-goals.ts --manager="manager_id" --distribute
```
